name: Docker Image CI

on:
    push:
        paths:
            - Dockerfile
            - dockerCore/*
        branches: [ master ]
    pull_request:
        paths:
            - Dockerfile
            - dockerCore/*
        branches: [ master ]

jobs:

    build:

        runs-on: ubuntu-latest

        steps:
            -   uses: actions/checkout@v2
            -   name: Build the Docker image
                run: docker build . --file Dockerfile --tag heroyt/laser-arena-control:$(date +%s)
